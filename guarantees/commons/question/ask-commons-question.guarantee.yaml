schemaVersion: treeseed.guarantee/v1
id: guarantee.commons.question.ask-commons-question.133
journeyIndex: 133
type: commons
subtype: question
journey: Ask Commons Question
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Ask Commons Question.
status: planned
dependencies:
  journeys: []
  guarantees: []
actors:
  allowed:
    - commons_participant
    - commons_steward
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes: []
scene:
  required: true
  manifest: ./scenes/ask-commons-question.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /commons/question/ask-commons-question
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.commons.question.ask-commons-question.api
content:
  required: true
  verifierRefs:
    - todo.commons.question.ask-commons-question.content
audit:
  required: true
  verifierRefs:
    - todo.commons.question.ask-commons-question.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.commons.question.ask-commons-question.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Ask Commons Question.
