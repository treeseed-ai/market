schemaVersion: treeseed.guarantee/v1
id: guarantee.commons.decision.record-commons-steward-decision.137
journeyIndex: 137
type: commons
subtype: decision
journey: Record Commons Steward Decision
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Record Commons Steward Decision.
status: active
dependencies:
  journeys:
    - 136
  guarantees:
    - guarantee.commons.proposal.vote-on-commons-proposal.136
actors:
  allowed:
    - commons_participant
    - commons_steward
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 136"
scene:
  required: true
  manifest: ./scenes/record-commons-steward-decision.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /commons/proposals/seed-proposal
  componentContract: record-action service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.commons.decision.record-commons-steward-decision.api
content:
  required: true
  verifierRefs:
    - market.commons.decision.record-commons-steward-decision.content
audit:
  required: true
  verifierRefs:
    - market.commons.decision.record-commons-steward-decision.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - market.commons.decision.record-commons-steward-decision.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Record Commons Steward Decision.
