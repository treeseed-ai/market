schemaVersion: treeseed.guarantee/v1
id: guarantee.public-profile.profile.set-profile-visibility.097
journeyIndex: 97
type: public-profile
subtype: profile
journey: Set Profile Visibility
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Set Profile Visibility.
status: planned
dependencies:
  journeys:
    - 95
  guarantees:
    - guarantee.public-profile.profile.view-public-user-profile.095
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 95"
scene:
  required: true
  manifest: ./scenes/set-profile-visibility.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /u/:username/set-profile-visibility
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.public-profile.profile.set-profile-visibility.api
content:
  required: false
  verifierRefs:
    - todo.public-profile.profile.set-profile-visibility.content
audit:
  required: true
  verifierRefs:
    - todo.public-profile.profile.set-profile-visibility.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.public-profile.profile.set-profile-visibility.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Set Profile Visibility.
