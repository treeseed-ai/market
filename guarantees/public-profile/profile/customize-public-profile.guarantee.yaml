schemaVersion: treeseed.guarantee/v1
id: guarantee.public-profile.profile.customize-public-profile.096
journeyIndex: 96
type: public-profile
subtype: profile
journey: Customize Public Profile
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Customize Public Profile.
status: planned
dependencies:
  journeys:
    - 95
  guarantees:
    - guarantee.public-profile.profile.view-public-user-profile.095
actors:
  allowed:
    - verified_user
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 95"
scene:
  required: true
  manifest: ./scenes/customize-public-profile.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /u/:username/customize-public-profile
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.public-profile.profile.customize-public-profile.api
content:
  required: false
  verifierRefs:
    - todo.public-profile.profile.customize-public-profile.content
audit:
  required: true
  verifierRefs:
    - todo.public-profile.profile.customize-public-profile.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - todo.public-profile.profile.customize-public-profile.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Customize Public Profile.
