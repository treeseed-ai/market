schemaVersion: treeseed.guarantee/v1
id: guarantee.checkout.cart.view-cart.115
journeyIndex: 115
type: checkout
subtype: cart
journey: View Cart
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for View Cart.
status: active
dependencies:
  journeys:
    - 99
  guarantees:
    - guarantee.marketplace.discovery.browse-marketplace.099
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 99"
scene:
  required: true
  manifest: ./scenes/view-cart.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /cart
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.checkout.cart.view-cart.api
content:
  required: false
  verifierRefs:
    - market.checkout.cart.view-cart.content
audit:
  required: true
  verifierRefs:
    - market.checkout.cart.view-cart.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.checkout.cart.view-cart.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for View Cart.
