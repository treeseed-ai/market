schemaVersion: treeseed.guarantee/v1
id: guarantee.checkout.billing.view-billing-state.120
journeyIndex: 120
type: checkout
subtype: billing
journey: View Billing State
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for View Billing State.
status: planned
dependencies:
  journeys:
    - 118
  guarantees:
    - guarantee.checkout.entitlement.view-entitlement.118
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 118"
scene:
  required: true
  manifest: ./scenes/view-billing-state.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /checkout/view-billing-state
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.checkout.billing.view-billing-state.api
content:
  required: false
  verifierRefs:
    - todo.checkout.billing.view-billing-state.content
audit:
  required: true
  verifierRefs:
    - todo.checkout.billing.view-billing-state.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - todo.checkout.billing.view-billing-state.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for View Billing State.
