schemaVersion: treeseed.guarantee/v1
id: guarantee.checkout.entitlement.view-entitlement.118
journeyIndex: 118
type: checkout
subtype: entitlement
journey: View Entitlement
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for View Entitlement.
status: planned
dependencies:
  journeys:
    - 117
  guarantees:
    - guarantee.checkout.payment.confirm-payment-group.117
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 117"
scene:
  required: true
  manifest: ./scenes/view-entitlement.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /checkout/view-entitlement
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.checkout.entitlement.view-entitlement.api
content:
  required: false
  verifierRefs:
    - todo.checkout.entitlement.view-entitlement.content
audit:
  required: true
  verifierRefs:
    - todo.checkout.entitlement.view-entitlement.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - todo.checkout.entitlement.view-entitlement.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for View Entitlement.
