schemaVersion: treeseed.guarantee/v1
id: guarantee.checkout.payment.confirm-payment-group.117
journeyIndex: 117
type: checkout
subtype: payment
journey: Confirm Payment Group
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Confirm Payment Group.
status: active
dependencies:
  journeys:
    - 116
  guarantees:
    - guarantee.checkout.cart.start-checkout.116
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 116"
scene:
  required: true
  manifest: ./scenes/confirm-payment-group.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /checkout/seed-checkout
  componentContract: record-action service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.checkout.payment.confirm-payment-group.api
content:
  required: false
  verifierRefs:
    - market.checkout.payment.confirm-payment-group.content
audit:
  required: true
  verifierRefs:
    - market.checkout.payment.confirm-payment-group.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.checkout.payment.confirm-payment-group.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Confirm Payment Group.
