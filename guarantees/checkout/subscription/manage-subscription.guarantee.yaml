schemaVersion: treeseed.guarantee/v1
id: guarantee.checkout.subscription.manage-subscription.119
journeyIndex: 119
type: checkout
subtype: subscription
journey: Manage Subscription
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Manage Subscription.
status: active
dependencies:
  journeys:
    - 118
  guarantees:
    - guarantee.checkout.entitlement.view-entitlement.118
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 118"
scene:
  required: true
  manifest: ./scenes/manage-subscription.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /checkout/seed-checkout
  componentContract: record-operations service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.checkout.subscription.manage-subscription.api
content:
  required: false
  verifierRefs:
    - market.checkout.subscription.manage-subscription.content
audit:
  required: true
  verifierRefs:
    - market.checkout.subscription.manage-subscription.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.checkout.subscription.manage-subscription.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Manage Subscription.
