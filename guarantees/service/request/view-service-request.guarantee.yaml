schemaVersion: treeseed.guarantee/v1
id: guarantee.service.request.view-service-request.122
journeyIndex: 122
type: service
subtype: request
journey: View Service Request
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for View Service Request.
status: active
dependencies:
  journeys:
    - 121
  guarantees:
    - guarantee.service.request.create-service-request.121
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 121"
scene:
  required: true
  manifest: ./scenes/view-service-request.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /services/seed-request
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.service.request.view-service-request.api
content:
  required: false
  verifierRefs:
    - market.service.request.view-service-request.content
audit:
  required: true
  verifierRefs:
    - market.service.request.view-service-request.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.service.request.view-service-request.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for View Service Request.
