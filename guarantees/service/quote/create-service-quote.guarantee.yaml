schemaVersion: treeseed.guarantee/v1
id: guarantee.service.quote.create-service-quote.123
journeyIndex: 123
type: service
subtype: quote
journey: Create Service Quote
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Create Service Quote.
status: active
dependencies:
  journeys:
    - 122
  guarantees:
    - guarantee.service.request.view-service-request.122
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 122"
scene:
  required: true
  manifest: ./scenes/create-service-quote.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /services/seed-request
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.service.quote.create-service-quote.api
content:
  required: false
  verifierRefs:
    - market.service.quote.create-service-quote.content
audit:
  required: true
  verifierRefs:
    - market.service.quote.create-service-quote.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.service.quote.create-service-quote.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Create Service Quote.
