schemaVersion: treeseed.guarantee/v1
id: guarantee.marketplace.templates.publish-template.101
journeyIndex: 101
type: marketplace
subtype: templates
journey: Publish Template
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Publish Template.
status: planned
dependencies:
  journeys:
    - 32
  guarantees:
    - guarantee.project.project.create-project.032
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 32"
scene:
  required: true
  manifest: ./scenes/publish-template.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /marketplace/publish-template
  componentContract: full-page-form guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.marketplace.templates.publish-template.api
content:
  required: true
  verifierRefs:
    - todo.marketplace.templates.publish-template.content
audit:
  required: true
  verifierRefs:
    - todo.marketplace.templates.publish-template.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - todo.marketplace.templates.publish-template.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Publish Template.
