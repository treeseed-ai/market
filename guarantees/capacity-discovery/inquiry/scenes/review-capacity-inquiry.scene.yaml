schemaVersion: treeseed.scene/v1
id: capacity-discovery.inquiry.review-capacity-inquiry.130
title: Review Capacity Inquiry
description: Scene scaffold for guarantee.capacity-discovery.inquiry.review-capacity-inquiry.130. This scene is intentionally minimal until the workflow-specific acceptance steps are authored.
audience:
  - operator
  - evaluator
journey:
  kind: service
  proves:
    - Review Capacity Inquiry opens the canonical @treeseed/market record operations workflow.
    - Review Capacity Inquiry exercises a service journey instead of only loading /capacity/seed-capacity-provider.
    - Review Capacity Inquiry records reviewable evidence for the visible result before activation.
  minimumSteps: 2
  requiresInteractiveAction: true
  consumesState:
    - key: capacity-provider.primary
      kind: capacity-provider
mode:
  test: true
  demo: true
  training: true
target:
  app: market
  environment: local
  baseUrl: auto
  browser: chromium
  viewport:
    width: 1600
    height: 900
devices:
  defaultProfile: desktop
  profiles:
    - id: desktop
      title: Desktop
      orientation: landscape
      viewport:
        width: 1600
        height: 900
      video:
        width: 1600
        height: 900
      output:
        width: 1920
        height: 1080
      deviceScaleFactor: 1
      isMobile: false
      hasTouch: false
setup:
  dev:
    required: true
    command: trsd dev start --web-runtime local --json
    reuseExisting: true
artifacts:
  trace: true
  video: true
  screenshots: true
  console: true
  network: true
  timeline: true
  appLogs: true
workflow:
  - id: open-entry-route
    title: Open Review Capacity Inquiry
    action:
      goto: /capacity/seed-capacity-provider
    expect:
      urlIncludes: /capacity/seed-capacity-provider
  - id: inspect-service-surface
    title: Inspect Review Capacity Inquiry service surface
    action:
      keyboard: Tab
    expect:
      urlIncludes: /capacity/seed-capacity-provider
chapters: []
overlays: []
diagrams: []
render:
  remotion:
    composition: treeseed-training-default
    output:
      format: mp4
      fps: 30
      resolution:
        width: 1920
        height: 1080
    capture:
      viewport:
        width: 1600
        height: 900
      video:
        width: 1600
        height: 900
      evidenceFit: fixed-browser
runtime:
  mode: training
  timeouts:
    sceneSeconds: 300
    chapterSeconds: 120
    stepSeconds: 60
  checkpoints:
    enabled: true
    defaultResumable: true
    everyStep: true
  progress:
    heartbeatSeconds: 15
  failure:
    continueOnFailure: false
training:
  formats:
    - json
    - markdown
    - vtt
    - srt
visualAudit:
  enabled: false
