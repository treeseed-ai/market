schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity-discovery.inquiry.review-capacity-inquiry.130
journeyIndex: 130
type: capacity-discovery
subtype: inquiry
journey: Review Capacity Inquiry
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Review Capacity Inquiry.
status: active
dependencies:
  journeys:
    - 129
  guarantees:
    - guarantee.capacity-discovery.inquiry.submit-capacity-inquiry.129
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 129"
scene:
  required: true
  manifest: ./scenes/review-capacity-inquiry.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /capacity/seed-capacity-provider
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.capacity-discovery.inquiry.review-capacity-inquiry.api
content:
  required: false
  verifierRefs:
    - market.capacity-discovery.inquiry.review-capacity-inquiry.content
audit:
  required: true
  verifierRefs:
    - market.capacity-discovery.inquiry.review-capacity-inquiry.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.capacity-discovery.inquiry.review-capacity-inquiry.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Review Capacity Inquiry.
