schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity-discovery.inquiry.submit-capacity-inquiry.129
journeyIndex: 129
type: capacity-discovery
subtype: inquiry
journey: Submit Capacity Inquiry
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Submit Capacity Inquiry.
status: active
dependencies:
  journeys:
    - 128
  guarantees:
    - guarantee.capacity-discovery.listing.view-capacity-listing.128
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 128"
scene:
  required: true
  manifest: ./scenes/submit-capacity-inquiry.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /capacity/seed-capacity-provider
  componentContract: full-page-form service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.capacity-discovery.inquiry.submit-capacity-inquiry.api
content:
  required: false
  verifierRefs:
    - market.capacity-discovery.inquiry.submit-capacity-inquiry.content
audit:
  required: true
  verifierRefs:
    - market.capacity-discovery.inquiry.submit-capacity-inquiry.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.capacity-discovery.inquiry.submit-capacity-inquiry.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Submit Capacity Inquiry.
