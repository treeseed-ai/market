schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity-discovery.listing.browse-capacity-listings.127
journeyIndex: 127
type: capacity-discovery
subtype: listing
journey: Browse Capacity Listings
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for Browse Capacity Listings.
status: planned
dependencies:
  journeys:
    - 99
  guarantees:
    - guarantee.marketplace.discovery.browse-marketplace.099
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 99"
scene:
  required: true
  manifest: ./scenes/browse-capacity-listings.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /capacity/browse-capacity-listings
  componentContract: dashboard/read-model guarantee harness
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - todo.capacity-discovery.listing.browse-capacity-listings.api
content:
  required: false
  verifierRefs:
    - todo.capacity-discovery.listing.browse-capacity-listings.content
audit:
  required: true
  verifierRefs:
    - todo.capacity-discovery.listing.browse-capacity-listings.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - todo.capacity-discovery.listing.browse-capacity-listings.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for Browse Capacity Listings.
