schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity-discovery.listing.view-capacity-listing.128
journeyIndex: 128
type: capacity-discovery
subtype: listing
journey: View Capacity Listing
ownerPackage: "@treeseed/market"
summary: First-pass product guarantee for View Capacity Listing.
status: active
dependencies:
  journeys:
    - 127
  guarantees:
    - guarantee.capacity-discovery.listing.browse-capacity-listings.127
actors:
  allowed:
    - buyer
    - seller
    - market_operator
  forbidden:
    - unauthorized_user
devices:
  required:
    - desktop_chromium
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Journey dependencies must be satisfied: 127"
scene:
  required: true
  manifest: ./scenes/view-capacity-listing.scene.yaml
  mode:
    acceptance: true
    demo: true
    training: true
  entryRoute: /capacity/seed-capacity-provider
  componentContract: dashboard/read-model service journey
  expectedEvidence:
    - playwright_trace
    - scene_video
    - screenshot_on_failure
    - console_log
    - network_log
api:
  required: true
  verifierRefs:
    - market.capacity-discovery.listing.view-capacity-listing.api
content:
  required: false
  verifierRefs:
    - market.capacity-discovery.listing.view-capacity-listing.content
audit:
  required: true
  verifierRefs:
    - market.capacity-discovery.listing.view-capacity-listing.audit
negativeCases:
  - id: unauthorized-actor-denied
    actor: unauthorized_user
    verifierRefs:
      - market.capacity-discovery.listing.view-capacity-listing.negative
evidence:
  required:
    - playwright_trace
    - scene_video
    - api_verification_log
    - audit_event_id
notes:
  - First-pass product guarantee for View Capacity Listing.
